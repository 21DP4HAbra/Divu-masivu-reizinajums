// Online C++ compiler to run C++ program online
#include <iostream>

int main() {
    std::string text;
    std::cout << "Ievadi vārdu: " << std::endl;
    std:: cin >> text;
    std::string str=text;
    int i;
    for(i = text.length() - 1; i >= 0; i--){
        std::cout<< text[i];
        
    }
    return 0;
}
